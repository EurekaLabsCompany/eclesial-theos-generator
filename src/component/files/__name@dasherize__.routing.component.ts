import { Routes, RouterModule } from '@angular/router';
import { ModuleWithProviders } from '@angular/core';

export const routes: Routes = [{}];

export const <%= classify(name) %>Routing: ModuleWithProviders = RouterModule.forChild(routes);
